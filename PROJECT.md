# 综合项目：任务管理 API

实现一个不依赖 Web 框架的领域层，之后可接入你熟悉的 Express/Fastify/NestJS。

## 需求

1. `Task` 有 `id`、`title`、创建时间，以及 `todo` / `doing` / `done` 三种状态；只有 `done` 有完成时间。
2. 创建接口接收 `unknown`，空标题返回结构化校验错误，禁止直接断言。
3. 仓储接口是泛型或专用接口均可，先实现内存版本。
4. 服务层提供创建、状态迁移、按状态分页查询。
5. 使用可辨识联合表达成功/失败，不以抛异常表示可预期的业务失败。
6. 对状态迁移做穷尽检查，并为边界解析、合法/非法迁移写测试。

## 验收标准

- `strict` 下类型检查通过，不使用显式 `any`。
- 删除或新增一个状态时，编译器能指出所有需要同步修改的位置。
- 所有不可信输入都先保持为 `unknown`。
- 能解释每个类型只提供编译期保障，还是也有对应的运行时检查。

进阶：增加乐观锁版本号；用 `satisfies` 检查状态处理器表；将 JS 版仓储用 `allowJs` + `checkJs` 渐进迁移。
